before_script:
  - export RUST_BACKTRACE=1
jdk:
  - oraclejdk8
language: rust
matrix:
  allow_failures:
    - rust: nightly
rust:
  - stable
  - beta
  - nightly
os:
  - linux
  # TODO: find out where `libjvm` is on Travis.
  #- osx
